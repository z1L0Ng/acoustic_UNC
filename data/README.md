# Data

Do not commit raw clinical data or identifiable recordings.

For preliminary experiments, prefer public or explicitly de-identified datasets. Store only documentation, download instructions, schemas, and small synthetic examples in Git. Raw, private, clinical, and generated data directories are ignored by default.

